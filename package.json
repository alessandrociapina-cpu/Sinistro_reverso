{
  "name": "sinistro-reverso",
  "version": "5.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run validate:data && npm run test:browser",
    "validate:data": "node scripts/validate-data.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "http-server": "^14.1.1"
  }
}
